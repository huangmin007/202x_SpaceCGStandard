# 2026-07-14

## 完成：RPC Server/Client 注释文档更新

### 代码注释修改（仅注释，未修改代码逻辑）
- **RpcServerBase.cs**：更新类摘要注释，修正 `ProcessInvokeSemaphore` 许可数注释（24→25），补充 `ProcessClientMessageAsync`、`WriteResponseMessageAsync` 等方法参数文档，修正 region 注释
- **RpcMessages.cs**：修正 `InvokeMessage`/`ResponseMessage` 构造函数注释（去除"内部"措辞），修正 `Id` 默认值注释，更新 `InvokeMessageEventArgs` 注释
- **RpcClientBase.cs**：补充 `HandleSessionAsync`、`WaitResponseAsync`、`WriteAsync` 参数文档，更新类摘要
- **RpcServer4X.cs**：完善类注释
- **RpcClient4X.cs**：修正示例代码中 `ConnectAsync()`→`Connect()`

### MD 文档更新
- **RPC 服务框架.md**：更新事件名 `ClientMessageInvoking`→`ClientInvokeRequest`，更新状态码表格，更新文件结构引用（`InvokeMessage.cs`+`ResponseMessage.cs`→`RpcMessages.cs`），修正 `CacheInstanceMethods`→`CacheObjectMethods`，更新客户端示例，更新已知局限
- **远程过程调用(XML-RPC)消息协议.md**：更新服务端错误码，新增客户端本地错误码表格，修正 Code 示例
- **README.md**：更新事件名、API 签名、状态码表格、文件结构、客户端示例

### MEMORY.md 更新
- 同步更新错误码约定
