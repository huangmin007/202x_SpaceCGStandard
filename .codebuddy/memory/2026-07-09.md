# 2026-07-09

## Net 命名空间注释整理

整理了 `SpaceCG.Standard/Net/` 目录下全部 5 个 .cs 文件的 XML 文档注释：

| 文件 | 整理内容 |
|------|----------|
| `RPCServerBase.cs` | 类摘要扩展（抽象基类说明）、`_syncContext` 注释补充、`_clients` 注释、构造函数/`RegisterObject`/`Start`/`Stop`/`AcceptTcpClientAsync`/`ReadTcpClientAsync`/`CallInvokeMessageAsync`/`TryCallMethod`/`GetInvokeMethods` 参数和返回值完整注释；修复 `ParseInvokeMessage` 参数顺序与签名一致；移除 `Stop()` 不存在的 `<exception>` 声明 |
| `InvokeMessage.cs` | 类摘要扩展、`ClientEndPoint` 添加注释、内部构造函数注释、`ToString()` 替换 `<inheritdoc/>` 为实际描述、三个 `Create` 重载的参数和返回值完整注释；`InvokeMessageEventArgs` 类及构造函数注释完善 |
| `InvokeResult.cs` | 类摘要扩展、属性注释完善、内部构造函数和两个 `Create` 工厂方法的 XML 文档注释补充；`ToString()` 注释改进 |
| `RPCServer4X.cs` | 扩展方法类添加注释、`RPCServer4X` 类摘要从 "RPC for XML" 扩展为完整描述、`ParseInvokeMessage` 和 `ResponseInvokeMessage` 添加 `<summary>` 说明 |
| `RPCClient.cs` | `TcpClientExtensions` 类添加注释、`IsConnected` 参数和返回值完整注释、`RPCClient` 类添加注释 |

## RPCServer4X.ParseInvokeMessage 重构

重构为字节级逐个扫描 + `yield return` 模式：
- 通过字节逐位扫描 `XMLEndFlags`（"/> "）定位完整 XML 元素边界
- 每个完整元素独立 `XElement.Parse` + `ToInvokeMessage`，单个失败不影响其他
- 尾部不完整数据自动跳过，由 `RPCServerBase` 环形缓冲后续补全再处理
- 天然支持单行多个 XML 元素
