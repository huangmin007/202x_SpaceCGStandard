<Window x:Class="Z_TestWpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Z_TestWpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="Window_Loaded" >
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Canvas x:Name="Canvas_Leds" Margin="0">
                <Canvas.Resources>
                    <Style TargetType="Rectangle">
                        <Setter Property="Width" Value="260" />
                        <Setter Property="Height" Value="19" />
                        <Setter Property="Fill" Value="#00002A33"/>
                        <Setter Property="StrokeThickness" Value="0"/>
                        <Setter Property="VerticalAlignment" Value="Top" />
                        <Setter Property="HorizontalAlignment" Value="Left" />
                    </Style>
                </Canvas.Resources>
                <d:Rectangle Fill="Red" Canvas.Left="20" />
            </Canvas>
        </Grid>
        
        <Grid Grid.Column="1">
            <Rectangle Name="Rectangle_0" Fill="Blue" Margin="100" />

            <TextBox Text="Hello...." Name="TextBox_0" Width="100" Height="60" />
        </Grid>
        
    </Grid>
</Window>
